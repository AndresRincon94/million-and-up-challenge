import { css } from 'styled-components';

const gridStyle = {
  wrapper: css`
    display: flex;
    flex-wrap: wrap;
    gap: 15px;
    justify-content: space-between;
  `
};

export default gridStyle;
